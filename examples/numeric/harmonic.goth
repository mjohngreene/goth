# Harmonic number: H(n) = 1 + 1/2 + 1/3 + ... + 1/n
# Precondition: n >= 1
# Postcondition: result ≈ ln(n) + γ (Euler-Mascheroni constant)
# Growth: H(n) ≈ ln(n) + 0.5772...
# Famous: Diverges, but slowly

╭─ recip : I64 → F64
╰─ 1.0 / (toFloat ₀)

╭─ harm : I64 → F64
╰─ if ₀ < 1 then 0.0
   else (recip ₀) + harm (₀ - 1)

╭─ main : I64 → F64
╰─ harm ₀
