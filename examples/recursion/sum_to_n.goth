# Sum of 1 to n (triangular numbers)
# Precondition: n >= 0
# Postcondition: result = n*(n+1)/2
# Property: sum(n) = n + sum(n-1) for n > 0
# Closed form: n*(n+1)/2
╭─ main : I64 → I64
╰─ if ₀ < 1 then 0
   else ₀ + main (₀ - 1)
