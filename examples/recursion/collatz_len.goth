# Collatz sequence length (3n+1 problem)
# Input: n
# Precondition: n > 0
# Postcondition: result >= 0
# Conjecture: Always terminates (unproven!)
# Property: If n=1, length=0
# Property: If n even, continue with n/2
# Property: If n odd, continue with 3n+1
# Famous: One of mathematics' unsolved problems

╭─ collatz : I64 → I64
╰─ if ₀ = 1 then 0
   else if (₀ % 2) = 0 then 1 + collatz (₀ / 2)
   else 1 + collatz (3 × ₀ + 1)

╭─ main : I64 → I64
╰─ collatz ₀
