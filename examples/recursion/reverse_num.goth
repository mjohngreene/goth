# Reverse digits of a number
# Precondition: n >= 0
# Postcondition: result >= 0
# Property: reverse(reverse(n)) = n (when no leading zeros)
# Example: reverse(1234) = 4321, reverse(100) = 1

╭─ rev : I64 → I64 → I64
╰─ if ₁ = 0 then ₀
   else rev (₁ / 10) (₀ × 10 + ₁ % 10)

╭─ main : I64 → I64
╰─ rev ₀ 0
